
26/05:
- Ball container -> Added to ballHandler.
- Ball Container used to drop balls into.
- Ball Handler -> Now controls the adding of a new Ball.
- Next step -> make balls move

Next steps:
- Move the RAF to ballContainer. ball can still use .move() so RAF loops throough and calls .move for each.
- Give container element to ballHandler -> add el to field.
- Use container to get bounds.
- ballContainer can have a collision detection.

Ball Manager:
- Knows position of all balls.
- responsible for determining if collision has happened.
- Gives updated velocities to ball after collision (later be outsourced to utility perhaps)

Balls:
- Does not know that a collision has happened 
- Can move itself using its own velocity and position.


25/05:
- Get balls to bounce back from edge.
-> Handle all internally inside ball. Simply invert the colliding axis direction
-> the position calculation doesn't work doing it this way. I need an origin and start time for origin. essentially it'll calculate time in this direciton. Little bit tricky with transform formula as this uses the div css location as origin 

Today:
- Managed to get ball shooting in x and y directions.
- Learning is to use .style instead of transform as it becomes complex when direction changes.
- Began migration to ballHandler so that I can have all movements in one update cycle and I can check for collisions.
- Classes should be PascalCase otherwise the names will clash between class blueprint and instance.
- Much more comofrtable with object structuring and destructuring with function arguments. Feels natural now.

22/05
Dodge the button.
Claude mentioned its css based and wasnt sure why.

Current thinking:
Mouse position can be found as an x y position.
Target object can be created using simple element
Target distance from mouse can be calculated using xy
Target position can be moved using distance.

Unsure of
- How to change Target position on screen
- Whether this needs canvas for x y or I can just use DOM tree
- Im unsure why using the style style.top and style.left worked while style.x y didn't work.

Current understanding:
- its all horizontal blocks

Steps:
1. Create Target object and try to change its position
- Did this via css Styles tag.

Learning:
- CSS controls positioning via top left right bottom in styles. Also has relative, static etc.
- Absolute css positioning appears to act as though the element is on a different z-axis so that it can be placed anywhere regardless of underlying element positioning.
- I can convert numbers to pixels using template literals
